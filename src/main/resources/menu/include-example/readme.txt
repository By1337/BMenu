В этой папке представлен пример использования `include`.

- В файле `items.yml` содержатся предметы.
- Файл `seller.yml` импортирует `items.yml` и получает доступ к ссылкам на предметы из этого файла.
- Аналогично, файл `confirm.yml` также импортирует `items.yml`.